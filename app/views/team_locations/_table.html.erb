<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th colspan=2>Teams</th>
      </tr>
    </thead>

    <tbody>
      <% @teams.each do |team| %>
        <tr>
          <td width="15%"><%= team.try(:number) %></td>
          <td width="85%"><%= team.try(:name) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= will_paginate @team_locations, renderer: BootstrapPagination::Rails %>

